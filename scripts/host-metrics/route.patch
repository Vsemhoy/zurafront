--- routes/web.php
+++ routes/web.php
@@ -48,2 +48,3 @@
 Route::prefix('api')->middleware(['spa.request', 'auth', 'active'])->group(function (): void {
+    Route::get('/monitoring/storage', [\App\Http\Controllers\Api\MonitoringController::class, 'show'])->middleware('throttle:30,1');
     Route::get('/scopes', [ScopeController::class, 'index']);
